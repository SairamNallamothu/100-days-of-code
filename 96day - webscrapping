import requests, re
from bs4 import BeautifulSoup

url = "https://news.ycombinator.com/"

response = requests.get(url)
html = response.text

soup = BeautifulSoup(html, "html.parser")

myLinks = soup.find_all("a", string=re.compile(r"(Eggs|books)"))
                        
#myLinks = soup.find_all("a", {"class":"css-1m051bw"})
print(len(myLinks))
counter = 0
for link in myLinks:
    print(link.text) #print(f"""https://www.yelp.com{link["href"]}""")
